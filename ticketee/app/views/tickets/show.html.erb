<div id="ticket">

  <div class="page-header">
    <h2><%= @project.name %></h2>
  </div>

  <div class="page-header">
    <%= link_to "Edit Ticket", [:edit, @project, @ticket], class: "edit pull-right" %>
    <h3><%= @ticket.title %></h3>

  </div>

    <%= simple_format(@ticket.description) %>

</div>