
<div class="page-header">
  <%= link_to "New Project", new_admin_project_path, class: "new pull-right" %>

  <h2>Projects</h2>

</div>

<div id="projects">

  <% @projects.each do |project| %>
    <h3><%= link_to project.name, project %></h3>
      <p><%= project.description %></p>
  <% end %>

</div>